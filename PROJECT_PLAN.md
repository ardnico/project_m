# プロジェクト計画 (PROJECT_PLAN.md)

## 1. プロジェクト概要

- 一文サマリ:  
  - IG証券のAPIを用いて、裁量トレードのルールを自動実行しつつ、リスクを定量管理できるFX自動売買システム
- 想定ユーザー:
  - 自分自身（にこ）
  - 将来的には、同等レベルの技術背景を持つ個人トレーダー
- 背景 / なぜ作るか:
  - 裁量トレードでは、感情・判断ブレ・疲労によるミスが発生しやすい  
  - エントリー/イグジットのルールは定義できるが、「その通りに淡々と実行する」ことが難しい  
  - 手動トレードはチャート監視の拘束時間が長く、生活リズム・メンタルに悪影響が出る  
  - 自分が言語化したトレード戦略を、機械に任せて検証・運用するための基盤が必要

---

## 2. ゴール / 非ゴール

### 2.1 ゴール（成功条件）

- G1: 手動ではチャート監視〜注文実行までに数分を要する作業を、「戦略ロジックの判断から数秒以内」に自動実行できること  
- G2: IG証券のデモ口座で、24時間連続稼働を7日間以上安定して行えること（例外で落ちない・自動復帰する）  
- G3: すべての注文・約定・残高変動・戦略判断をロギングし、後から検証できる履歴をDB/ファイルに残すこと  
- G4: 一定のリスク制御（1日あたり最大損失額・最大ポジション数・証拠金維持率）をコードで強制できること  
- G5: 戦略ロジック部分を Python の関数単位でテスト可能な形に分離し、最低限の単体テストがグリーンであること  

### 2.2 非ゴール（やらないこと）

- NG1: 初期フェーズでの本番口座運用で「勝てること」の保証  
  - まずは **デモ口座での安定稼働とロジック検証** を優先する
- NG2: 高頻度取引（HFT）レベルのレイテンシ最適化  
  - IG API とインターネット経由である時点で、数ms単位の最適化は意味が薄い
- NG3: マルチユーザー対応・権限管理・Web GUI などのSaaS的機能  
  - 当面は「自分用」のシングルユーザーシステム
- NG4: 複数ブローカー対応（OANDA, GMO, etc.）  
  - 第一段階では IG証券専用とし、マルチブローカー化は将来検討
- NG5: バックテストエンジンの自作  
  - バックテストは基本的に外部ツール/既存ライブラリ or CSVベース簡易実装で対応し、「本番執行基盤」を優先

---

## 3. 制約

- 言語:
  - メイン: Python 3.11 以降
  - 将来拡張候補: Rust（注文執行エンジンの一部を置き換える可能性）
- 対応 OS:
  - 開発環境: Windows 10 以降（WSL2上のUbuntu推奨）
  - 本番運用: Linuxサーバ（Ubuntu LTS想定）
- 外部サービス / API:
  - IG証券 REST API + Streaming (Lightstreamer)  
  - 通知用にメール or Discord / Slack Webhook など（MVPではメール程度でも良い）
- ビルド / 実行:
  - Python: `poetry` or `pip` + `venv` で環境固定
  - プロセス管理: `systemd` などのOSレベルサービス管理（将来）
- 想定開発期間:
  - 戦略ロジックが既にある前提で、**4〜6週間程度でMVP（デモ口座での自動売買開始）**
- 運用制約:
  - 回線は常時接続回線（自宅回線＋無停電電源などは今後検討）
  - 「落ちたら即死」のようなハイレバ設定は前提にしない

---

## 4. 全体アーキテクチャ（ざっくり）

CLIツールではなく「常駐型の自動売買サービス」として設計しつつ、構造は分割しておく。

- エントリポイント:
  - `src/main.py`  
    - 設定読み込み  
    - ログ設定  
    - 各コンポーネント（データ取得、戦略、注文、リスク管理）の起動
- モジュール構成（例）:
  - `src/config/`
    - 設定スキーマ定義（pydantic など）
    - 設定ファイル読み込み（YAML or TOML）
    - IG APIキー、取引対象通貨ペア、ロットサイズ、リスクパラメータなど
  - `src/infra/`
    - `ig_client.py` : REST/Streamingクライアントラッパ
    - `db.py` : SQLite / PostgreSQLアクセス層
    - `mailer.py` : アラートメール送信
    - `logging_setup.py` : ログフォーマット、ローテーション設定
  - `src/domain/`
    - `models.py` : 注文、ポジション、シグナル等のドメインモデル
    - `strategy/` : 個々の戦略実装（クラス or 関数）
    - `risk.py` : リスクチェックロジック（1日損失上限、ポジション上限など）
  - `src/app/`
    - `price_streamer.py` : 価格配信購読・キューへの流し込み
    - `strategy_runner.py` : 価格ストリームからシグナル生成
    - `order_executor.py` : シグナルを受け注文発注・約定管理
    - `scheduler.py` : 起動・停止・再接続などのオーケストレーション
- テスト:
  - 単体テスト: `tests/unit/`（戦略ロジック、リスクチェック、設定バリデーション）
  - 結合テスト: `tests/integration/`（IGデモ口座への疎通、ダミーデータを用いた一連のフロー）

---

## 5. 優先機能（MVPスコープ）

- M1: **IGデモ口座への接続・認証**
  - REST API を使ってログイン・口座情報取得ができる
  - シンプルなGET/POSTのラッパを実装 or 既存ライブラリのラップ
- M2: **リアルタイム価格取得（最低限ポーリングでも可）**
  - 対象通貨ペアのBid/Askを定期取得（例: 1秒〜数秒間隔）
  - 取得した価格と時刻をDB/ログに記録
  - Streaming (Lightstreamer) の導入はMVP後半〜第二フェーズ
- M3: **単一戦略のシグナル生成**
  - 例: シンプルな移動平均クロス、ボリンジャーバンドブレイクなど  
  - 今ある裁量ルールをコードに落としたものを1つ実装
  - シグナル生成部分は純粋関数にし、過去データでも検証可能な形に
- M4: **注文実行と約定管理**
  - 売買シグナルに基づき、IG REST API で新規/決済注文を発行
  - 約定情報を取得し、ポジション一覧と一致させて管理
  - すべての注文リクエスト/レスポンスをログ・DBに保存
- M5: **リスクガード（最低限版）**
  - 1日あたりの累計損益が一定閾値を超えたら、その日は自動売買停止
  - 証拠金維持率が一定値を下回る前に新規注文を止める
- M6: **エラー処理と自動復帰（最低限版）**
  - APIエラー時のリトライ（指数バックオフなど）
  - プロセス再起動時に、「現在のポジションを読み取り、矛盾した状態でスタートしない」ようにする
- M7: **シンプルな監視・通知**
  - 重要イベント（一定損失到達、API障害、プロセス異常終了）をメール or Webhookで通知

---

## 6. 非機能要件

- パフォーマンス:
  - 通常の自宅回線＋標準的なPC環境で、  
    - 価格取得 → シグナル判定 → 注文リクエスト までが数秒以内に完了すればOK  
    - Pythonのオーバーヘッドは許容範囲とする
- 信頼性:
  - 例外発生時には、スタックトレースをログ出力しつつ、必要なら自動復帰を試みる  
  - 回線切断 / API一時障害時は即座に終了せず、一定時間リトライする
- メンテナンス性:
  - 戦略ロジックは他のインフラ層から切り離し、バックテスト用コードからも再利用できるようにする  
  - 注文ロジック・リスクロジックも個別モジュールとしてテスト可能にする
- ログ・監視:
  - ログレベル（info/debug/warn/error）を設定ファイルで切り替え可能  
  - 重要イベントと通常ログを分ける（例: `app.log`, `trades.log`）  
  - 将来的にPrometheusなどへのメトリクス出力ができるような構造を意識（MVPでは未実装でも可）

---

## 7. リスクと対応

- R1: **戦略そのものの期待値がマイナスであるリスク**
  - 対応:
    - デモ口座で十分な期間（最低数週間〜数ヶ月）検証する  
    - ロジック変更のたびにバージョンを管理し、どのロジックでどの成績だったかを分離して記録
- R2: **API仕様変更・IG側の制限（レート制限・接続数制限など）**
  - 対応:
    - APIクライアント層を1箇所に集約し、仕様変更時の修正範囲を局所化  
    - レート制御の仕組みをクライアント層に持たせる（リクエスト間隔・キューイング）
- R3: **インフラ障害（ネットワーク切断・PC再起動など）**
  - 対応:
    - 再起動時のポジション同期ロジックを実装  
    - 「注文状態をDBに書いてから送る」「約定後にステータス更新」といった二重化で整合性を担保
- R4: **ソフトウェアのバグによる異常注文・過剰ロット**
  - 対応:
    - 「ロットサイズ上限」「1トレード当たり許容損失額」「1日当たり最大新規回数」などを設定で制限  
    - 本番口座移行前に、異常な入力（NaN, 極端な価格など）に対するテストを実施
- R5: **開発側のスコープ肥大（バックテスト、GUI、複数戦略などを同時にやりたくなる）**
  - 対応:
    - PROJECT_PLAN と実際のMVPスコープを明示し、「まずデモ口座で単一戦略を安定稼働させる」ことを優先  
    - 新機能はすべて「第二フェーズ」以降のExecPlanとして切り出す

---

## 8. 開発フロー

- 全体方針:
  - **「戦略の勝ち負け」より先に「システムが安定して動き続けること」を最優先**  
  - 小さな単位で動くものを作り、段階的に複雑さを上げていく

- 流れの例:
  1. `PROJECT_PLAN.md`（本ドキュメント）を確定  
  2. `execplans/001-ig-connection.md`  
     - IGデモ口座接続と価格取得のための詳細設計  
  3. `execplans/002-single-strategy-signal.md`  
     - 単一戦略の実装とテスト設計  
  4. `execplans/003-order-execution-and-logging.md`  
     - 注文フローとログ/DB構造の詳細  
  5. `execplans/004-risk-guard-v1.md`  
     - リスク制御のルールと実装方針  
  6. `execplans/005-monitoring-and-alert.md`  
     - 通知・監視の仕組み
- Codex / 自動生成ツールの使い方:
  - `.agent/AGENTS.md` に「このプロジェクトでの役割・ルール」を明記  
  - 各ExecPlan単位で、「編集してよいファイル範囲」「目的」「完了条件」を明示してからツールに投げる  
  - 生成コードは必ずレビュー・テストを通してから main ブランチにマージ

---

## 9. オープンな検討事項

- Q1: **設定ファイル形式**
  - 候補: YAML / TOML / JSON
  - 人間が手で書く頻度が高いので YAML or TOML が有力
  - ツール連携を考えると JSON もありだが、可読性重視なら外す方向
- Q2: **価格取得方式**
  - MVPでは「RESTポーリング」で十分か、それとも最初からStreamingを使うか
  - ポーリング間隔とレート制限・ネットワーク負荷のバランス
- Q3: **戦略ロジックのバージョニング方法**
  - 単にGitのコミットで管理するのか、DBに「strategy_version」を持たせるか
- Q4: **Rust導入タイミング**
  - Pythonで運用が安定した段階で、「注文エグゼキュータだけRustに置き換える」価値があるか
  - どの程度のトラフィック／戦略数になったらマイクロサービス化を検討するか
- Q5: **バックテストとの一貫性**
  - 本番の戦略コードをそのまま使ってバックテストできる構造にするか
  - それとも「本番は堅牢優先、バックテストは別実装」と割り切るか

---

## 10. レビュー所見（冷徹な現実チェック）

- **運用手順の欠落**: サービスの起動・停止・デプロイ手順が不明なまま。「誰かがCLIを叩けば動く」前提では、再起動後の整合性保証や検証手順が抜け落ちる。CI/CDや手動手順の最小セットを今すぐ明文化しないと、トラブル時に自分でも再現できない。
- **監視・復旧の曖昧さ**: 再接続やリトライの方針はあるが、どのエラーを致命的とみなすかの基準がない。例外ハンドリングポリシーとヘルスチェックの定義を決めないと「意図せず止まっていた」が常態化する。
- **リスクガードの境界条件未定義**: 「1日最大損失」などが挙がっているが、閾値の算定根拠と計算周期が書かれていない。日付の切り替え、サマータイム、週跨ぎなどの境界条件が未定義のままでは実装時に迷走する。
- **ログ／データ保持期間の決め打ちなし**: どの粒度・期間のログを残すのか、DB肥大化やローテーションの方針がない。後から付け足すと本番停止が必要になるので、最低限の保持設計をMVPから決める。
- **テスト戦略の薄さ**: ユニットと結合テストの言及だけで、モック方針やダミーデータの作り方が未提示。外部API依存が強いので、通信層のフェイクをどう差し替えるか決めないとテストが形骸化する。
- **オペレーション上の権限・秘密情報扱い**: APIキーや秘密情報の保管場所、ローテーション手順が不明。個人利用でも漏洩リスクは同じなので、最初から管理方法を固定しないと後で抜け道が常態化する。

### 10.1 今すぐ埋めるべきギャップ（優先順位付き）

1. **運用 Runbook の提示**: 起動・停止・再起動・デプロイ・ロールバックの最短手順を、コマンド列と前提条件付きで1ページにまとめる。手順がない状態で稼働を始めるのは事故の呼び水。
2. **ヘルスチェックと致命エラー基準の決定**: 例外クラス・HTTPステータス・ストリーミング切断など、何を致命とみなして即時停止／再起動するかを列挙し、監視対象メトリクス（心拍、キュー滞留、APIレスポンス時間など）を決める。
3. **リスクガードの閾値表と日次リセット規則**: 「日次損失上限」「ポジション数上限」「証拠金維持率下限」を数値で決め、UTC/JSTどちらでリセットするか、週末・祝日・サマータイムの扱いを明記する。
4. **ログ／データ保持方針の初期値確定**: 保存するログ種別（app/trades/audit）、ローテーション閾値（サイズ or 日数）、DBのアーカイブ／パージ方針を設定し、閾値を設定ファイルで切り替えられる形にする。
5. **テストの差し替え戦略**: IG API のフェイク層（録画リプレイ or in-memory stub）を決め、ユースケース別に「実APIなしで再現できるか」を表にする。モック方針がないままではテストが書けず、品質は運任せのまま。
6. **秘密情報の管理ルール**: APIキーの保管場所（例: `.env` + OS権限、シークレットマネージャ）、共有・ローテーション手順、ログへの混入防止策（構造化ログでマスク）を決めて、プロジェクトルートに「絶対にコミットしないファイル」を明示する。

