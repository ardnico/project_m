# プロジェクト計画 (PROJECT_PLAN.md)

## 1. プロジェクト概要

- 一文サマリ:  
  - IG証券APIを利用し、裁量トレードのルールを自動実行しつつ、リスクを数値で強制管理できる自動売買システム。
- 想定ユーザー:
  - 自分自身（にこ）
  - 将来的には同程度の技術背景を持つ個人トレーダー
- 背景 / なぜ作るか:
  - 感情・疲労・判断ブレによるミストレードを排除したい  
  - 裁量ルールは定義できるが人間が常時監視するのは非現実的  
  - ロジックの改善・検証を継続するための「安定運用基盤」が必須  
  - FX自動取引の“本質的ボトルネック”は戦略より運用と再現性であり、その弱点を解消するため

---

## 2. ゴール / 非ゴール

### 2.1 ゴール（成功条件）

- G1: 戦略判断 → 注文発行が数秒以内に自動実行できる  
- G2: IGデモ口座で 24h × 7日 連続稼働しても落ちず、切断時は自動復帰できる  
- G3: すべての価格データ・注文・約定・損益を DB/ログに永続化し、後から検証可能  
- G4: 1日最大損失・ポジション上限・証拠金維持率などのリスク管理をコードで強制  
- G5: 戦略ロジック部分が純粋関数としてテスト可能であり、unit-test がグリーン  
- G6: **（追加）運用手順が明確化されている**（起動/停止/再起動/デプロイ）

### 2.2 非ゴール（やらないこと）

- NG1: MVP段階での「勝てる戦略」の保証  
- NG2: HFT（ミリ秒単位最適化）  
- NG3: マルチユーザー / Web GUI / SaaS 化  
- NG4: 複数ブローカー対応  
- NG5: バックテストエンジンのゼロからの自作  
- NG6: **（追加）高度なクラウド監視基盤（Prometheus, Grafana, K8s）**  
  - 将来対応に含めるがMVPには入れない

---

## 3. 制約

- 言語:
  - メイン: Python 3.11+
  - 拡張候補: Rust（執行エンジンの高信頼化）
- 対応OS:
  - 開発: Windows + WSL2  
  - 本番: Linux (Ubuntu LTS)
- 外部サービス:
  - IG証券 REST / Streaming API  
  - 通知: メール or Webhook
- 実行 / 管理:
  - Python仮想環境（venv or poetry）  
  - systemd / Supervisor による常駐  
  - **APIキーは.env または OS の秘密情報管理機構に保存（後述）**
- 想定開発期間:
  - 4〜6週間でMVP

---

## 4. 全体アーキテクチャ（ざっくり）

- `src/main.py`  
  起動処理・設定読込・ロギング・コンポーネント起動。
- `src/config/`  
  pydantic を使った設定スキーマ、YAML/TOML の読込、環境変数マージ。
- `src/infra/`
  - `ig_client.py`: REST + Streaming（後半）  
  - `db.py`: SQLite → 将来PostgreSQL  
  - `secrets.py`: APIキー取得、ローテーション手順  
  - `logging_setup.py`: ログローテーション、フォーマット
- `src/domain/`
  - `models.py`: 注文/ポジション/シグナル  
  - `strategy/`: 戦略ロジック  
  - `risk.py`: リスク管理（閾値・期間定義・日付切替処理を含む）
- `src/app/`
  - `price_streamer.py`: ポーリング or Streaming  
  - `strategy_runner.py`: シグナル生成  
  - `order_executor.py`: 注文発注・整合性管理  
  - `scheduler.py`: 再接続・再起動制御・ヘルスチェック
- テスト
  - `tests/unit/`（戦略・リスク・設定・エラー処理）  
  - `tests/integration/`（APIフェイク差替え、注文フロー模擬）

---

## 5. 優先機能（MVPスコープ）

- M1: IGデモ口座の認証と口座情報取得  
- M2: **RESTポーリングによる価格取得（秒単位、Streamingは第二フェーズ）**  
- M3: 単一戦略のシグナル生成  
- M4: 新規/決済注文と約定情報の永続化  
- M5: **境界条件込みのリスクガード（1日損失上限、日付切替、週跨ぎ、サマータイム）**  
- M6: エラー分類に基づく自動復帰（通信/認証/注文失敗の再試行ポリシー）  
- M7: メール通知（異常終了・リスク到達）  
- M8: **サービス起動/停止の操作手順書の作成**

---

## 6. 非機能要件

- パフォーマンス:
  - 価格取得→判定→注文リクエストが 3秒以内  
  - ポーリング間隔は 1〜5秒（APIレート制限考慮）
- 信頼性:
  - エラー種別を以下に分類しポリシーを定義  
    - 通信エラー（再試行）  
    - APIリジェクト（ポリシーに応じて停止 or 間隔延長）  
    - 認証エラー（再ログイン、失敗続く場合は停止）  
    - 致命例外（プロセス停止＋通知）  
  - ヘルスチェック（内部状態確認・API疎通確認）
- メンテナンス性:
  - 戦略ロジックは副作用なしの関数に切り分け  
  - DBスキーマとエラーハンドリングを明示的に定義  
- ログ:
  - `app.log`（INFO以上）  
  - `debug.log`（詳細）  
  - `trades.log`（注文・約定のみ）  
  - **ログ保持期間：1〜3ヶ月 / ローテーション日次**

---

## 7. リスクと対応

- R1: 戦略の期待値が未知  
  - デモ運用で数週間以上のデータ/可視化を収集  
  - strategy_version をDBに持たせ検証可能に
- R2: API仕様変更  
  - クライアント層の分離、ラッパーメソッドの最小化
- R3: インフラ障害  
  - 再起動時に「現在のポジション」をAPIから読み出し同期  
  - 最後に送信した注文IDをDBから読み復旧
- R4: バグによる過剰注文  
  - ロット上限、トレード回数上限、損失上限を設定で強制  
- R5: スコープ肥大  
  - すべての新機能は ExecPlan に切り出す
- R6: **（追加）秘密情報の漏洩**
  - APIキーがファイルに平文で残らないよう管理  
  - `.env` + OS秘密ストア  
  - Git管理外に固定し、ローテーション手順を明文化

---

## 8. 開発フロー

- ① 本PROJECT_PLAN.mdを確定  
- ② `execplans/001-ig-connection.md`（接続・ポーリング詳細）  
- ③ `execplans/002-strategy-v1.md`（戦略ロジックとテスト）  
- ④ `execplans/003-order-exec.md`（注文フロー・整合性）  
- ⑤ `execplans/004-risk-guard.md`（境界条件込み）  
- ⑥ `execplans/005-monitoring.md`（通知・ヘルスチェック）  
- ⑦ **運用手順書 v1（起動/停止/デプロイ/復旧）**
- ⑧ GitHub Actions or ローカルCIで自動テスト  
- Codex の利用:
  - `.agent/AGENTS.md` と ExecPlan を読み込ませ、**編集範囲を限定**  
  - モジュール単位で生成し、必ずテストとlintを通す

---

## 9. オープンな検討事項

- Q1: **設定ファイル形式**
  - 候補: YAML / TOML / JSON
  - 人間が手で書く頻度が高いので YAML or TOML が有力
  - ツール連携を考えると JSON もありだが、可読性重視なら外す方向
- Q2: **価格取得方式**
  - MVPでは「RESTポーリング」で十分か、それとも最初からStreamingを使うか
  - ポーリング間隔とレート制限・ネットワーク負荷のバランス
- Q3: **戦略ロジックのバージョニング方法**
  - 単にGitのコミットで管理するのか、DBに「strategy_version」を持たせるか
- Q4: **Rust導入タイミング**
  - Pythonで運用が安定した段階で、「注文エグゼキュータだけRustに置き換える」価値があるか
  - どの程度のトラフィック／戦略数になったらマイクロサービス化を検討するか
- Q5: **バックテストとの一貫性**
  - 本番の戦略コードをそのまま使ってバックテストできる構造にするか
  - それとも「本番は堅牢優先、バックテストは別実装」と割り切るか

---

## 10. レビュー所見（冷徹な現実チェック）

- **運用手順の欠落**: サービスの起動・停止・デプロイ手順が不明なまま。「誰かがCLIを叩けば動く」前提では、再起動後の整合性保証や検証手順が抜け落ちる。CI/CDや手動手順の最小セットを今すぐ明文化しないと、トラブル時に自分でも再現できない。
- **監視・復旧の曖昧さ**: 再接続やリトライの方針はあるが、どのエラーを致命的とみなすかの基準がない。例外ハンドリングポリシーとヘルスチェックの定義を決めないと「意図せず止まっていた」が常態化する。
- **リスクガードの境界条件未定義**: 「1日最大損失」などが挙がっているが、閾値の算定根拠と計算周期が書かれていない。日付の切り替え、サマータイム、週跨ぎなどの境界条件が未定義のままでは実装時に迷走する。
- **ログ／データ保持期間の決め打ちなし**: どの粒度・期間のログを残すのか、DB肥大化やローテーションの方針がない。後から付け足すと本番停止が必要になるので、最低限の保持設計をMVPから決める。
- **テスト戦略の薄さ**: ユニットと結合テストの言及だけで、モック方針やダミーデータの作り方が未提示。外部API依存が強いので、通信層のフェイクをどう差し替えるか決めないとテストが形骸化する。
- **オペレーション上の権限・秘密情報扱い**: APIキーや秘密情報の保管場所、ローテーション手順が不明。個人利用でも漏洩リスクは同じなので、最初から管理方法を固定しないと後で抜け道が常態化する。

### 10.1 今すぐ埋めるべきギャップ（優先順位付き）

1. **運用 Runbook の提示**: 起動・停止・再起動・デプロイ・ロールバックの最短手順を、コマンド列と前提条件付きで1ページにまとめる。手順がない状態で稼働を始めるのは事故の呼び水。
2. **ヘルスチェックと致命エラー基準の決定**: 例外クラス・HTTPステータス・ストリーミング切断など、何を致命とみなして即時停止／再起動するかを列挙し、監視対象メトリクス（心拍、キュー滞留、APIレスポンス時間など）を決める。
3. **リスクガードの閾値表と日次リセット規則**: 「日次損失上限」「ポジション数上限」「証拠金維持率下限」を数値で決め、UTC/JSTどちらでリセットするか、週末・祝日・サマータイムの扱いを明記する。
4. **ログ／データ保持方針の初期値確定**: 保存するログ種別（app/trades/audit）、ローテーション閾値（サイズ or 日数）、DBのアーカイブ／パージ方針を設定し、閾値を設定ファイルで切り替えられる形にする。
5. **テストの差し替え戦略**: IG API のフェイク層（録画リプレイ or in-memory stub）を決め、ユースケース別に「実APIなしで再現できるか」を表にする。モック方針がないままではテストが書けず、品質は運任せのまま。
6. **秘密情報の管理ルール**: APIキーの保管場所（例: `.env` + OS権限、シークレットマネージャ）、共有・ローテーション手順、ログへの混入防止策（構造化ログでマスク）を決めて、プロジェクトルートに「絶対にコミットしないファイル」を明示する。

